import React from "react";

// parsing is like dissecting or understanding information by breaking it into smaller pieces that can be easily processed or manipulated by a computer program
const ParseHTML = () => {
  return <div>ParseHTML</div>;
};

export default ParseHTML;
